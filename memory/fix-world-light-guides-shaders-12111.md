---
name: fix-world-light-guides-shaders-12111
description: Deferred IRLights world guide overlay after Iris on MC 1.21.11.
type: project
---
09-18 USER: guides/gizmo invisible with shaders on1.21.11, possibly other versions. Asked whether light contours vs BBS transform arrows and film vs form preview; no answer yet.
Scope: IRLights PointLight/Spotlight world contours on port/1.21.11 in _wt-addon-1.21.11. BBS arrows, standalone editor,1.20.x/1.21.1 unchanged. Other-version runtime unknown.
Cause from local BBS2.6/Iris1.10.7 code: guides draw through Draw.flushTrianglesNoDepth shared POSITION_COLOR pipeline during world entity pass. No Iris program assignment; draw lands in client framebuffer and later pack composite overwrites it. Old versions use global position-color ShaderProgram; same failure not established there.
FIX: WorldLightGuideOverlay snapshots local pose/global modelview, frame world projection GpuBufferSlice, copied color and scalar shape. Only world=true + BBSRendering.isIrisWorldForms enqueue; offscreen FramebufferForm children remain immediate. Spotlight captureGuideMatrix stays at original draw for drag math. GameRenderer.render AFTER renderWorld invocation flushes after Iris world composite and final color conversion, before HUD/BBS preview capture. Direct GPU colour-only pass into current mc framebuffer, no depth test/write, translucent blending; no global matrix mutations. Snapshot queue cleared each frame and finally after flush. Preview/picking/no-shader paths unchanged. No native buffers retained between passes.
Validation: gradlew build PASS; ASM check real GameRenderer.render/renderWorld descriptors + unique call, projection API/BBS gate + required injection PASS. JAR175classes <=major65,exact release core1.21.11,7patches,mixin class inventory PASS. SpotGuideDrag/SpotGuideKeyframes byte-identical to previous autokeyframes release. GPU/game visual check NOT run, user runtime OPEN. API harness in build/guide-overlay-check (untracked build output).
Artifact: Desktop/IRLights-1.1.7-shader-guides/irlite-1.1.7+mc1.21.11.jar plus README/verification.json. SHA256 8de32ed1ebbb43cd0eec63f892b6ec0b667d870eed967a8a1a7f6dd46c89405e. No commit/push. MavenLocal restored to release core1.20.x/JDK17 after packaging. Core source unchanged.
Runtime check: shaders ON/OFF,PointLight+Spotlight world,selected film replay,drag rings+auto-keyframes,form preview. Ordinary BBS arrows require separate diagnosis if user confirms those missing too.

09-18 FOLLOW-UP user requested code-only audit other MC; NO source fixes/builds/launches in this turn.
Checked1.20.1/1.20.4(master shared source),1.21.1(port source); actual Iris1.7.2+1.20.1,1.7.2+1.20.4,1.8.8+1.21.1-fabric binaries via javap. All overridePositionColorShader -> ShaderKey.BASIC_COLOR; gate isRenderingWorld&&isMainBound; ExtendedShader.apply binds before/after-translucent Gbuffer framebuffer. Therefore1.21.11-specific unassigned RenderPipeline failure is absent in older3.
BUT analogous guide visibility failure exists by code: all old guides draw before pack deferred/composite with depthMask(false)+disableDepthTest; BBS film forms draw at solid-layer TAIL under shaders. Checked actual BBS dependency binaries;1.20.4/1.21.1 host JAR exact match,1.20.1 JAR differs but all4relevant classes BBSRendering/WorldRendererMixin/FilmControllerHud/Gizmo byte-identical to local source build. Subsequent world draws can overwrite guide pixels since guides leave no depth; pack lighting/fog/post effects also affect guides.
Concrete shader case: run/shaderpacks/Photon_IRLights_Replays/shaders/program/gbuffers_basic.fsh:99 writes guide data to Gbuffer; settings.glsl:611 enables separate-entity output only MC_VERSION>=260100 (not any reviewed MC). program/d4_deferred_shading.fsh:297 chooses draw_sky for depth==1, ignoring guide material. Thus a guide over untouched sky may disappear; exact runtime extent not measured. Same anchors in unmodified Photon. Complementary basic also invokes DoLighting, later deferred applies depth-driven fog/sky effects; no blanket claim all packs always hide guides.
Ordinary BBS film gizmo distinct: FilmControllerHud.render -> gizmo.renderGizmo -> renderInterface UI overlay after world, so no analogous early-world composite loss found. Code audit does not establish runtime absence of other bugs. Form-preview/stencil paths not identified with this world-pass failure.
Evidence dumps: main addon build/guide-code-audit (javap exact Iris+dependency BBS). Recommended future fix for world light guides on older3: version-adapted post-shader overlay; not implemented/ported without request. Previous1.21.11 patch remains runtime OPEN.

09-18 FIX/RELEASE CHECKPOINT: user explicitly requested fix on all supported addon MC + commit. Scope4MC1.20.1/1.20.4/1.21.1/1.21.11; not standalone editor1.21.4 or BBS arrows. Older3 now snapshot pose/modelview/projection/color/dimensions and flush AFTER GameRenderer.renderWorld inside render, before BBS captures preview. Gates: explicit world route + BBSRendering.isIrisWorldShadersEnabled (offscreen children excluded).1.20.x MatrixStack and1.21.1 Matrix4fStack adapted separately. Legacy flush restores projection/sorting/appliedMV+stack/shaderColor/shader/depthWrite/depthTest/cull/blendFactors/blendEnable/read+drawFBO/viewport in finally, queue clears at frame HEAD and after flush. No persistent native geometry.1.21.11 uses prior direct GPU pipeline implementation.
Build PASS all4. tools/verify-guide-overlay.ps1 + tools/profiles/GuideOverlayTest.java added all3branches; actual Minecraft/BBS bytecode checks exact render descriptors,unique invocation,required AFTER injection,real host shader gate. Headless real render-state snapshot tests: offscreen rejection,point+spot,mutable parent pose/camera/color isolation,dimensions,stale frame reset;20checks each old3 and19on1.21.11=79. Additional legacy runs onJVM17 PASS20each. Fixture substitutes only BBS shader-enabled flag; no GPU drawing/game runtime checked.1.21.11 projection is frame GPU slice,legacy projection copy tested.
All4JAR in Desktop/IRLights-1.1.7-shader-guides with README/verification.json; exact release core perMC+7patches+mixin inventory+recursive bytecode limits PASS. Classes including JiJ188/188/188/175;1.20.x max61(Java17),1.21.x max65(Java21).SpotGuideDrag/SpotGuideKeyframes byte-identical to previous autokeyframes release on eachMC. Older JAR1.21.11 in same folder replaced with verified build. Runtime OPEN; no claim of visual acceptance. Core sources unchanged;MavenLocal restored release1.20.x/JVM17;main Gradle classpath restored1.20.4. Source changes in master + port/1.21.1 + port/1.21.11, all within user's commit request;push not requested.
