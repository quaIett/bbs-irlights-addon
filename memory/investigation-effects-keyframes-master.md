---
name: investigation-effects-keyframes-master
description: "09-18 разведка master: числовые VL/Outline ключи работают; own-settings gates, пустые Outline targets и GPU-clamp объясняют неэффективные ключи; фикса нет."
metadata:
  node_type: memory
  type: project
---

2026-09-18. Запрос: подготовка master к релизу; разведать неработающие ключи секций Volumetric/Outline в фильме.
Контекст: основная папка addon/core остаётся на 2.0 с незакоммиченным профайлером. Работа выполнена в существующем CLEAN master ../_wt-addon-1.20.x (21e0ab5); branch switch в основной папке/stash/commit/push не делались. Java/UI/GLSL не менялись.

Подтверждённый механизм:
- LightEffects.java:25,39 custom_vl/custom_outline=false. Числовые поля всегда visible; переключатели invisible (97), поэтому секции предлагают ключи и при наследовании глобалов.
- LightEffects.isDefault():103 проверяет только переключатели. LightEffectsRegistration.apply():88,101 убирает профиль при default и отсутствии automatic replay selection, хотя numeric runtimeValue может быть ненулевым.
- apply():110 customVl берётся только из e.customVl.get(); apply():111 customOutline = automatic Outline selection OR customOutline. Numeric key сам режим не включает.
- Исключение: Outlined replays с mode=selected включает собственный outline автоматически. Пустой selected список = контур никому; НЕ объяснять такой случай только custom_outline=false.
- Front/Glow дополнительно требуют собственных boolean переключателей. Noise/Beam аналогично.
- LightProfile.write в release core clamp-ит значения. BBS runtime keys могут выходить за диапазон (ValueFloat.setRuntimeValue не clamp). Пример Noise amount=7.1 -> GPU=1; Fresnel=4.95 ->4; отрицательная Strength ->0.
- LightTrackLayout.decorate дополнительно скрывает vl_intensity и vl_max_dist из каталога, но сохранённые треки продолжают вычисляться. Отдельно от основной причины.

Проверки JDK21 / MC1.20.4 / BBS2.6:
- tools/verify-profiles-bbs.ps1 PASS1298 +11 API anchors; compileJava/compileClientJava UP-TO-DATE.
- Временный build/profiles-bbs-test/EffectsPlaybackProbe.java: 13 float параметров x Point/Spot, реальные FormUtils.copy, FormProperties save/load/apply/reset, LightEffectsRegistration, LightRegistry и LightProfile.write; PASS494. OFF -> профиль отсутствует; ON -> ключи 0/5/10/0 доходят до packed GPU payload. OpenGL/игра не запускались.
- build/profiles-bbs-test/SavedFilmEffectsProbe.java читает gzip BBS1, не пишет фильмы. Отчёты probe-report.txt и film-report.txt там же.
- run/config/bbs/data/films/1.dat основной папки: Point custom_vl=false/custom_outline=false; outline_replays mode=selected, список пустой. Эффективно customVl=false/customOutline=true, outlineTargets=0; Strength1.4; Noise7.1->1; Fresnel4.95->4.
- run/saves/Regular/bbs/films/1.dat: те же own flags, но Outline selected содержит 1146e111; Strength0:1.4,12:-16.1; на тике6 runtime=-7.3500004, packed=0; тике12 packed=0. Профиль outline=true/targets1, поэтому общей поломки числового playback здесь нет. Resolve целей и GPU-рендер в живом фильме не проверялись.
- Эти файлы обнаружены локально; пользователь не подтвердил, что это именно текущий проблемный фильм.

OPEN: уточнение включённых Own switches задано пользователю; ответа на момент записи нет. Для релиза предлагается согласовать UX: numeric runtime keys автоматически активируют соответствующий профиль без изменения persistent form switches, либо явно показывать неактивность дорожек; защитить UI от значений вне диапазона. Это предложение, НЕ реализация/не согласованный план. Если сбой воспроизводится при own ON, валидных значениях и корректных targets, нужен конкретный фильм/шейдер для следующей runtime-разведки. Общего отказа числовых треков до упаковки GPU не обнаружено.
2026-09-18 ПРОДОЛЖЕНИЕ: пользователь разрешил исправление релизного master (релиз ещё не опубликован).
Реализация: LightEffectsRegistration.apply читает наличие numeric runtimeValue у 8 VL+5 Outline полей; активирует только соответствующую секцию. Noise-ключи включают noise, все VL-ключи beam; Outline-ключи outline, front/glow strength дополнительно соответствующие flags. Saved переключатели не меняются. Empty selection остаётся Nobody, собственные переключатели сохраняются после сброса. BBS numeric extrapolation до первого/после последнего ключа сохранена.
UI: подписи Own settings объясняют автоматическое включение ключами. LightEffects: комментарии уточнены. GLSL/core Java/формат сериализации не менялись. Верхние/нижние ограничения значений и скрытые vl_intensity/vl_max_dist не менялись.
Регрессия tools/profiles/ProfilesBbsTest.java: реальный BBS save/load/copy/playback -> LightRegistry -> LightProfile.write; 13 параметров x2формы; 0/5/10/0, equal-to-default, удалить все ключи, reset, независимость/совместность секций, вручную включённый профиль, Nobody/INHERIT, выключенные beam/noise/outline, default-off front/glow. До фикса падает numeric key activates profile: vl_intensity; после PASS2084+11anchors на обоих MC до финальной перепроверки release-core.
Готча теста: TrackBehaviours.clearOverrides сбрасывает спец overrides ModelForm, НЕ обычные numeric runtime values. Поэтому прямое удаление channel из FormProperties без reset не моделирует удаление ключей в UI; регрессия проверяет channel.removeAll() -> apply (PropertyTrack сбрасывает runtime), отдельно resetProperties. Не заявлять доказанный сброс при произвольном удалении channel из map.
Готча сборки: mavenLocal org.qualet:irl-core:1.1.7 содержал чужую сборку (RawOccluderBatch/ShadowRenderer$Capture, hash не равен main core). Восстановлено publishToMavenLocal из _wt-irl-core-1.20.x main на JDK17; финальные addon сборки/проверки повторяются. --refresh-dependencies --offline несовместим с текущим Loom download, обычный запуск после этого перестроил кеш.
ФИНАЛ: release core main publish на JDK17 PASS; addon master MC1.20.1 и1.20.4 build PASS,2084checks+11anchors КАЖДЫЙ; вложенный core обоих jar SHA256 идентичен _wt-irl-core-1.20.x/build/libs/irl-core-1.1.7.jar, class major61 PASS. Выдача _wt-addon-1.20.x/build/release-effects-keyframes/irlite-1.1.7+mc1.20.{1,4}.jar; рядом checks и bundle-report.json. diff --check PASS. Runtime игровой OPEN у пользователя. Без commit/push; порты1.21.x и2.0 не менялись.

2026-09-18 ЗАПУСК ПО ЗАПРОСУ «не ворктрее»: основная папка bbs-irlights-addon реально переключена с2.0 на master. Незакоммиченный профайлер2.0 сохранён git stash a478b30824cb2ca5fc9f0a24cb1eff020ab596e1 (включая untracked). Фикс master сохранён stash6dff7499c11e21bc430bbac0f40120a2af93ef85 и применён в основной папке; оба stash НЕ удалены. _wt-addon-1.20.x освобождён switch --detach на21e0ab5, теперь чистый без фикса. Сборки в его build/release-effects-keyframes остаются. Метаданные восстановления: основная папка build/master-client-checkout-backup.json. Core основная папка остаётся2.0+незакоммиченные изменения, не трогали; mavenLocal содержит release core main1.1.7.
Запуск из основной папки: run/launch-master-client.sh -> Git Bash/JDK21 -> ./gradlew runClient -Pmc=1.20.4 --console=plain; лог run/runclient-console.log. Моды из dev sources, run/mods не содержит irlite/core jar. В логе загрузки подтверждены BBS2.6/IRLite1.1.7+mc1.20.4/core1.1.7, процесс752 с launch.cfg основной папки. Игровая проверка пользователем.
2026-09-18 THICKNESS: по сообщению пользователя об отсутствии толщины добавлено LightEffects.outlinePixelSize (outline_pixel_size, ValueFloat0..6, default0=global); регистрация включает numeric activation Outline и округляет положительные значения до целых pixelSize (>=1); 0 наследует глобал. Copy global settings копирует толщину. UI панели и timeline Outline показывают Thickness (0=global). Старые формы при ownOutline=true сохраняют динамическое наследование глобальной толщины, т.к. отсутствующее свойство0. Shader/core уже поддерживают per-light f.y (GPU offset84), Java core/GLSL не менялись. Формат float выбран для интерполяции: BBS Integer factory сериализует stepped канал (эксперимент1->5 на тике5 дал1), float даёт3; округление только при регистрации.
MC1.20.4: verify-profiles-bbs PASS2146+11anchors, build PASS, core JiJ hash совпадает с release main. Выдача в ОСНОВНОЙ папке build/release-effects-keyframes/irlite-1.1.7+mc1.20.4.jar (и build/libs); старые сборки в detached _wt-addon-1.20.x НЕ содержат thickness. Текущий открытый клиент НЕ перезапускался: увидит новый трек только после restart. 1.20.1 thickness ещё не пересобиралась; порты1.21.x не трогали; без commit/push.
2026-09-18 KEYFRAME SLIDERS: пользователь запросил ограниченные trackpad-sliders там, где выход за пределы не влияет на картинку, интенсивность оставить; уточнил ТОЛЬКО редактор ключей реплея. Реализованы LightKeyframeRanges (15идов), UILightFloatKeyframeFactory extends UIFloatKeyframeFactory и узкий LightKeyframeEditorMixin.createPanel HEAD. Own LightForm + ValueFloat + whitelist, без изменения фабрик прочих форм/типов/аддонов. Заменяется только поле value через существующий IrliteTrackpads -> Refreshed UISliderTrackpadAdapter; без Refreshed fallback ограниченный обычный UITrackpad. Наследуются G/editing/cancel/autokey/playhead/Bezier, callback повторяет штатный getEditTarget/setValue маршрут. Открытие legacy out-of-range ключей не пишет их в данные; ввод/жест/G ограничены, график/импорт/старые ключи не нормализуются массово.
Границы по реальному LightProfile.write: vl_intensity0..5 (скрытый legacy трек), vl_max_dist1..256, vl_tip_boost0..4, vl_tip_radius.01..4, vl_noise_amount0..1, vl_noise_scale.01..6, vl_noise_speed/morph0..3; outline_strength0..3, outline_pixel_size0..6(0=global), outline_fresnel.001..4 (фактический GPUmin, не form min1), outline_back0..2, front_strength0..1.5, glow_strength0...75; anisotropy-.95...95(GLSL). Обычные intensity/beam_strength/range/radius/density/bulb/cookie оставлены штатными свободными keyframe trackpads. Панели формы не менялись в этой итерации.
MC1.20.1+1.20.4 build PASS, profiles2232checks+12API anchors каждый (новый anchor createPanel), bundle обоих идентичен release main1.1.7. Оба свежих jar (включая thickness предыдущего шага) в ОСНОВНОЙ папке build/release-effects-keyframes. Клиент на момент сборки уже закрыт пользователем; повторно не запускался. UI/runtime ручная приёмка OPEN. Без commit/push, 1.21.x/2.0 не менялись.
09-18: после запуска клиента из основной папки пользователь запросил название трека outline_pixel_size ровно «Thickness» без скобок и коммит. Изменён только timeline label,form label сохранён. Проверка1.20.4 повторно PASS2232+12anchors. Коммит включает весь текущий фикс ключей/VL/Outline,толщину и ограниченные слайдеры;push не запрошен. Уже запущенный клиент требует следующего запуска для обновления подписи.
