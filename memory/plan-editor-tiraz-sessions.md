---
name: plan-editor-tiraz-sessions
description: "Согласованный план (2026-07-23) тиража РЕДАКТОРА (irl-editor) main→4 порт-ветки за 3 сессии: S1 1.20.1, S2 1.21.1+1.21.4, S3 1.21.11. Матрица core-staleness + toolchain + shadow-бэкенд по версиям; канон запушен."
metadata:
  node_type: memory
  type: project
  originSessionId: df4f4047-2923-4b1b-bb4e-7491f262cd00
  modified: 2026-07-23T14:47:37.419Z
---

Директива юзера 2026-07-23: СТАРТ Фазы B шаг 2 ([[plan-post-trilogy-port-rollout]]) — тираж РЕДАКТОРА (irl-editor, папка `BBS/irlights`, пакет `org.qualet.irlredactor`). main = ИСТОЧНИК тиража, ФИНАЛЕН. Заход 2 редизайна ([[project-editor-redesign]]) — группы источников «Пещера (4)»+DnD / beam-стиль Smoky / глобальный Softness — юзер ЯВНО ОТЛОЖИЛ до ПОСЛЕ тиража (не блокирует). Ветку под сессию НЕ создавать ([[feedback-no-per-session-branch]]); коммит по чекпоинтам ([[commit-checkpoints]]).

**Канон запушен 2026-07-23** (по решению юзера «пушим канон», чистый ff): core `origin/main = 6cebbc5` (ef76cdb..6cebbc5, cookie-mirror), editor `origin/main = 3b69a0d` (fbd3e6f..3b69a0d, +27: весь редизайн UI+free-cam+guide-overlay+slider). 0 unpushed обоих. Аддон master уже был запушен (b8a8fdc).

## Топология (факт git 2026-07-23)
- Editor: все 4 порт-ветки форк из ОДНОЙ точки `e147571`; main = **+41 editor-коммит**. ⇒ editor-мёрж (`merge main`) = ОДИН И ТОТ ЖЕ 41-дельта-набор на все ветки, тяжесть конфликтов ∝ render-дивергенции. Порт-ветки own-behind: 1.20.1=16, 1.21.1=31, 1.21.4=33, 1.21.11=51; changed_files 38–48. Editor main пинит irl-core:1.1.3 (коммит b8f6a83).
- Core-staleness (ГЛАВНЫЙ драйвер разбивки), mergebase с core main:
  - **1.20.1**: core = `main` (6cebbc5). 0 core-работы.
  - **1.21.1**: core `1.21.1`=127cd07, mb=ef76cdb → отстаёт **~1** (только cookie 6cebbc5). Весь 1.1.3 УЖЕ влит (сессия аддона 07-22).
  - **1.21.4**: core `1.21.4`=7eebbe6, mb=cf4ad94 → отстаёт **~32** (весь 1.1.3 fwd-port: cluster/VL-UBO/2D-atlas point-shadows/bake-track/VRAM). НУЖЕН core-мёрж; без него editor-редизайн (`setOutline`/`setShadow`) не скомпилится на этой линии.
  - **1.21.11**: core `1.21.11`=9d80fd7, mb=cf4ad94 → **~32 + reconcile против raw-GL** ShadowRenderer (RawOccluderBatch seam). Самое тяжёлое.
- Toolchain/Iris/shadow: 1.20.x = Loom1.9/JDK17/Iris1.7.2 (immediate real-model); 1.21.1+1.21.4 = Loom1.15.5/JDK21/Iris1.8.8 (2-arg, immediate); 1.21.11 = Loom1.15.5/JDK21/Iris1.10.7 (4-arg, **raw-GL**).

## Разбивка (юзер выбрал «3 сессии, моя схема», easy→hard)
- **S1 — 1.20.1** (+ jar 1.20.4 из main для верификации, что канон зелёный). core=main; editor-мёрж near-clean (тот же 1.20.x render-API); порт-специфика = 3 файла (dep-матрица + LWJGL-пин 3.3.1, см. [[project-port-1201]]). Warm-up процесса editor-мёржа перед дорогими core-мёржами.
- **S2 — 1.21.1 + 1.21.4** вместе (общий toolchain). Цепь core-мёржей: `main`→`1.21.1` (крошечный, ~1) → затем `1.21.1`(127cd07)→`1.21.4` (наследует резолюции 1.1.3, оставляет только 1.21.2/1.21.4 render-дельты: ProjectionType / ShaderProgramKeys.POSITION / getCullingShape() безаргум. / GlUsage — см. [[project-port-1214]]).
- **S3 — 1.21.11** отдельно. core main→1.21.11 переложить на raw-GL; Iris 1.10.7 4-arg; deferred-GUI ImGui после blitToScreen; input-рекорды. Референс = [[project-port-1211]] + [[project-port-12111-refresh]].

## Процесс каждой сессии (жёсткий порядок, per-MC core publish)
CORE-ветка → editor-мёрж → build → jar → ин-гейм. Конкретно: (1) `git -C irl-core checkout <core-ветка>` + при нужде merge main + resolve → `gradlew publishToMavenLocal` под правильным JDK (mavenLocal per-MC, перезаписывает предыдущую линию — republish обязателен перед сборкой ДРУГОЙ линии); (2) в редакторе `git merge main` в порт-ветку, resolve (port-only держат: dep-матрица/build.gradle, raw-GL/render-шимы, input-миксины); (3) `gradlew build` на порт-ветке; (4) jar в `Desktop/IRLights/<ver>/` (редактор JiJ-несёт core); (5) ин-гейм runClient — за юзером (Prism), локальный рантайм окружения нет. JDK: 17=`Eclipse Adoptium/jdk-17.0.19.10-hotspot`, 21=`jdk-21.0.11.10-hotspot`; системный JAVA_HOME=JDK8 нерабочий → задавать явно.

## ⚠️ ГОТЧА мёржа (выявлено в S1, повторится в S2/S3)
Порт-ветки несут **cherry-pick-КОПИИ** main-фич с ДРУГИМИ SHA (far-from-origin набор: double-позиции `dragValue(double[])`, 2-й inject после Camera.update, camera-relative `.irlights`, light-relative bake). mergebase `e147571` предшествует ОБЕИМ копиям ⇒ `git merge main` авто-сливает их РЯДОМ → **ДУБЛИКАТЫ методов/инжектов** (компайл-фейл: double `dragValue` ×2, Camera;update inject ×2 в Widgets.java/GameRendererLightMixin.java). Тихо, без conflict-маркеров. ПРАВИЛО РЕЗОЛЮЦИИ: main = надмножество-правда → любой файл, что тронули ОБЕ линии, резолвить **в main** (`git checkout main -- <file>` или `--theirs`), КРОМЕ реально version-specific (1.20.1: только build.gradle/gradle.properties/fabric.mod.json; 1.21.x: +render-шимы/raw-GL/input-рекорды). ВЕРИФИКАЦИЯ: `git diff --stat main` смёрженного дерева ДОЛЖЕН показывать ТОЛЬКО version-specific файлы — если всплыл source-файл, это дубль-гибрид, чинить в main. `.irlights` тоже брать main (== аддон; порт держит старые: complementary main/аддон 111775 vs порт 81794).

## Ход S1 — 1.20.1 ✅ DONE + PUSHED (2026-07-23, ин-гейм PASS у юзера)
core main republish (Loom1.9/JDK17, mavenLocal 1.1.3=1.20.x) → `git merge --no-commit main` в port/1.20.1 (был f59ed3f) → 7 конфликтов (6 .irlights → main; LightEditorPanel коммент → main) + 2 тихих дубль-гибрида (Widgets/GameRendererLightMixin → main) → финал `diff --stat main` = РОВНО 3 dep-файла (build.gradle 1.20.1-coords+LWJGL3.3.1-пин+core1.1.3, gradle.properties, fabric.mod.json ~1.20.1) → `gradlew build --refresh-dependencies` JDK17 **BUILD SUCCESSFUL 1m48s** (mixin lwjgl-noise benign) → jar `irl-redactor-1.1+mc1.20.1.jar` (8.06MB, JiJ ровно 1×irl-core-1.1.3, imgui-натив, 7 патчей) → Desktop/IRLights/1.20.1/. Ин-гейм PASS (Prism 1.20.1). **Закоммичено+запушено: `port/1.20.1 = b3a6e3a`** (f59ed3f..b3a6e3a, origin 0 unpushed). Следующая: S2 (1.21.1+1.21.4).

## Ход S2 — 1.21.1 + 1.21.4 ✅ DONE + PUSHED (2026-07-23, ин-гейм PASS у юзера)
Оба jar собраны (JDK21/Loom1.15.5/Iris1.8.8 2-arg), доставлены Desktop/IRLights/{1.21.1,1.21.4}/, РОВНО 1×irl-core-1.1.3 JiJ + 7 патчей + натив. **Ин-гейм PASS (Prism, клавиша J, БЕЗ jar аддона).** Запушено (все ff, 0 unpushed): core **1.21.1=3624676** (cherry-pick cookie 6cebbc5), core **1.21.4=f658e38** (merge 1.21.1); editor **port/1.21.1=4e9417f**, **port/1.21.4=b192fe6** (merge main). Worktree `_wt-editor-1.21.4` убран (gradlew --stop → remove → prune). Editor main-дерево осталось на port/1.21.1 (чисто). **Осталась S3 = 1.21.11 (последняя, самая тяжёлая: core raw-GL reconcile + Iris 1.10.7 4-arg).**
- **CORE**: 1.21.1 ← cherry-pick 6cebbc5 (cookie, 1 файл, чисто) → publish. 1.21.4 ← `merge 1.21.1`: 4 конфликта (FramePipeline/LightRegistry/ShadowBaker → 1.21.1 целиком = конвергентный coord-fix; ShadowRenderer → 1.21.1 + переналожить 14 render-шимов) + автомёрж BlockShadowCollector (готча: суперсет 1.21.1 добавил 2-й `getCullingShape(world,pos)` @L236 → тоже no-arg). diff vs ветки 1.21.1 = РОВНО 3 (build.gradle 1.21.4-toolchain + ShadowRenderer + BlockShadowCollector). compile против yarn1.21.4 = шимы верны. publish OK.
- **1.21.4 render-шимы (ФАКТ, для S3)**: ShadowRenderer 14 сайтов = VertexSorter→ProjectionType.PERSPECTIVE(×3 setProjectionMatrix)/getVertexSorting→getProjectionType/−applyModelViewMatrix()(×3)/GameRenderer::getPositionProgram→ShaderProgramKeys.POSITION/VertexBuffer.Usage.STATIC→GlUsage.STATIC_WRITE(×2)/savedSorter→savedProjectionType. BlockShadowCollector `getCullingShape()` no-arg(×2, getCollisionShape/getOutlineShape СОХРАНЯЮТ world,pos). Метод = взять 1.21.1-версию + механич. конверсия (light-relative bake = КОНВЕРГЕНТНА, уже в 1.21.1).
- **EDITOR (та же готча дубль-гибрида ×2 линии)**: Widgets `dragValue(double[])` авто-дублировался БЕЗ маркеров на ОБЕИХ линиях → checkout main. LightEditorPanel+6.irlights→main. GameRendererLightMixin→--ours(порт RenderTickCounter+getTickDelta(true)) + прививка VlProfiler(frameTick/beginPass PASS_BAKE/endPass) + убрать main-дубль uploadLights. diff main: **port/1.21.1 = 5** (build.gradle/gradle.properties/fabric.mod.json/LightGuideRenderer/GameRendererLightMixin); **port/1.21.4 = 9** (те + NOTES-1.21.4.md/RedactorEntityCasterSource/AutoLightManager/FreeCamInputMixin).
- **НОВЫЕ 1.21.2→1.21.4 API-ФАКТЫ (javap-подтверждено, критично для S3=1.21.11)**: (1) `World.isClient` = И `public final boolean isClient` (ПОЛЕ) И `public boolean isClient()` (МЕТОД) на 1.21.4 → обе формы компилятся → WorldBlockChangeMixin взял main (поле). (2) `BlockState.isOpaqueFullCube()` NO-ARG на 1.21.2+ (2-арг ДРОПНУТ) → AutoLightManager version-specific. (3) **Input-рефактор 1.21.2/3**: `Input.pressingForward/Back/Left/Right/jumping/sneaking` → УДАЛЕНЫ, заменены полем `playerInput` (record `net.minecraft.util.PlayerInput`{forward/backward/left/right/jump/sneak/sprint} + `PlayerInput.DEFAULT` все-false); `KeyboardInput.tick(ZF)V` → `tick()V` NO-ARG. movementForward/Sideways float ОСТАЛИСЬ. FreeCamInputMixin потребовал реальный фикс (`@Inject tick()V` + `self.playerInput = PlayerInput.DEFAULT`) — **free-cam НИКОГДА не проверялся ин-гейм на портах ([[project-editor-free-camera]] «порт-ветки open»); на 1.21.1 tick(ZF)V+pressing-поля ЕСТЬ → там мискин верен, jar 1.21.1 не трогать**. FreeCamera читает GLFW-хэндл (version-agnostic). (4) 1.21.4 render-эра = pre-RenderPipeline (raw-GL цел, как 1.21.1); Camera.getPos() ещё валиден (getCameraPos = 1.21.11).
- Готча демонов: worktree-сборка в общем ~/.gradle падала «daemon stopped» → пересобрать `--no-daemon` (реальный exit-код; `| tail` маскирует под 0).

## Ход S3 — 1.21.11 ✅ DONE + PUSHED (2026-07-23, ин-гейм PASS у юзера)
**ЗАВЕРШЕНО. Ин-гейм PASS. Запушено: core `1.21.11=3e6990c` (merge 9d80fd7+f658e38), editor `port/1.21.11=cdfad5e` (merge 46c7b4d+3b69a0d), addon `master=2e1b7b3` (+3 старых фикса допушено).** ⇒ ВЕСЬ ТИРАЖ РЕДАКТОРА (S1 1.20.1 + S2 1.21.1/1.21.4 + S3 1.21.11) ЗАКРЫТ. jar `irl-redactor-1.1+mc1.21.11.jar` 8.07MB (JiJ 1×core-1.1.3 + 7 патчей + imgui). Чистка веток (решение юзера 07-23): удалены `optimization/octahedral-point-shadows` (core+addon, ancestor canon) + addon `debug/cluster-heatmap` (уникальный WIP `25a10ab` — reflog-восстановим ~90д до gc; план/промпт остаётся в [[plan-cluster-heatmap-debug]]). Открыто: `bbs-dof-addon` БЕЗ remote (Phase B шаг 3 DoF — отдельная сессия по команде, remote завести до пуша).
- **⚠️ КЛЮЧЕВАЯ ГОТЧА S3 (mixin-apply, компайл+javap НЕ ловят)**: inherited-`@Shadow` protected-поля суперкласса не прикрепляется, если миксин таргетит ПОДКЛАСС. FreeCamInputMixin@Mixin(KeyboardInput) `@Shadow protected Vec2f movementVector` (объявлено в Input!) → крашнул ин-гейм на входе в мир: «@Shadow field field_55868 was not located in target class class_743». Фикс = `@Accessor("movementVector")` на `@Mixin(Input.class)` (класс-ОБЪЯВИТЕЛЬ) + `((InputAccessor)self).setMovementVector(Vec2f.ZERO)`; `playerInput` (public) — прямой каст. javap подтверждает СУЩЕСТВОВАНИЕ поля, но НЕ attach-ability @Shadow — правило: @Shadow только declared-in-target; inherited → @Accessor на declaring class. (Краш был при загрузке KeyboardInput на finish_configuration ⇒ все прочие миксины уже применились на старте, каскада нет.)
- **CORE** `1.21.11`: `git merge --no-commit 1.21.4` (НЕ main — 1.21.4 несёт 1.1.3-дельту на 1.21.x-API-базе) → 7 конфликтов. Модель реконсиля: raw-GL порт (3527d63) переписал ТОЛЬКО draw-слой ShadowRenderer; вся оркестрация/math байт-идентична → 1.1.3-фичи прививаются чисто. Резолв: **take THEIRS** (LightRegistry/LightBuffer/CookieArrayBase/IrlSamplers/SpotlightDepthAtlas/BlockShadowCache/IRLShadowQuality/OccluderSink/ShadowConfig/ShadowEngine/ShadowBaker + **PointShadowEvsm/Pyramid форс-override авто-мёржа**); **take OURS** (ShadowRenderer/IrlSamplersBind 4-arg/OccluderBatch); **грефты**: ShadowRenderer 3 (beginSpot tileSizePx, +restrictScissorSpot/debugFillScissoredDepth, beginPointFace cube→PointDepthAtlas ~6 строк), ShadowBaker +1 (releaseScratch в onShadersDisabled), FramePipeline ×4 (joml/ClusterGridBuffer.uploadEmpty/LightRegistry.prioritize/onGbufferMatricesCaptured), BlockShadowCollector THEIRS-хвост sameSilhouette+casterKind+remap cutout на BlockRenderLayers.CUTOUT; **import-flip** platform→opengl GlStateManager ×4 (DepthTileAtlas/BlueNoiseTexture/PointShadowEvsm/Pyramid); **DELETE** PointShadowArray+ImmediateOccluderBatch; gradle.properties→1.1.3. **Point-shadow ФАКТ: 2D-«октаэдр» = МИСНОМЕР — те же 6 cube-граней в 6 суб-ректов (FACE_COL{0,1,2,0,1,2}/FACE_ROW{0,0,0,1,1,1}), TIER_SUPERCELLS{2,3,1}@div1/2/4 = {2,12,16}=30 блоков; beginPointFace меняет ТОЛЬКО render-target (FBO+viewport/scissor).** compileJava SUCCESSFUL 24s; publishToMavenLocal 1.1.3=1.21.11-intermediary; ревью 5/5 PASS 0 находок. core `1.21.11` tip был 9d80fd7.
- **EDITOR** `port/1.21.11`: `git merge --no-commit main` → 14 конфликтов; diff --stat main = только version-specific. **5 ТИХИХ РЕВЕРТОВ (прошли diff-stat, упали компайл/рантайм)**: (1) FreeCamInputMixin — на 1.21.11 `movementForward/Sideways` floats **И** `pressing*/jumping/sneaking` bool УДАЛЕНЫ → `@Shadow protected Vec2f movementVector=Vec2f.ZERO` + `playerInput=PlayerInput.DEFAULT`, `tick()V` no-arg (форма ≠ и 1.20.4 main, и 1.21.4!); (2) CameraFreeMixin — `Camera.update` 1-й арг **World** (не BlockView) иначе mixin-apply краш; (3) FreeCamera/GuideOverlay `getPos()`→`getCameraPos()`, IRLRedactorClient free-cam keybind String→`CATEGORY`; (4) CompositeRendererTimerMixin — `FullScreenQuadRenderer.renderQuad()` УДАЛЁН на Iris 1.10.7 (квад через GpuBuffer) → endPass свёрнут в use()-redirect; (5) Widgets S2-дубль dragValue→main. Резолвы: WorldBlockChangeMixin `!isClient()||isOutOfHeightLimit(pos)` (main-логика+метод; isOutOfHeightLimit = default HeightLimitView); GameRendererLightMixin union(getTickProgress+VlProfiler)+удалён main-дубль uploadLights; LightGuideRenderer порт-рендер+main overlay-guard; RedactorEntityCasterSource порт raw-GL; AutoLightManager no-arg isOpaqueFullCube; 7 .irlights+LightEditorPanel→main; mixins.json union. build SUCCESSFUL 30s; ревью 3/3 PASS 0 находок (все 12 миксинов javap-verified). port `port/1.21.11` tip был 46c7b4d.
- **НОВЫЕ 1.21.11 API-ФАКТЫ (javap, для будущего)**: Camera.update(**World**,Entity,ZZF)V; Input = `protected Vec2f movementVector`+`public PlayerInput playerInput` (float-пара И pressing-bool ДРОПНУТЫ, ≠ 1.21.4); KeyboardInput.tick()V no-arg; PlayerInput.DEFAULT; Iris1.10.7 FullScreenQuadRenderer.renderQuad() УДАЛЁН (GpuBuffer); BlockModels.getModel→**BlockStateModel** (не BakedModel); World.isClient private-поле + isClient() метод + isOutOfHeightLimit(BlockPos) via HeightLimitView. **⇒ merge-tree предсказал конфликты точно; `git diff --stat main` НЕ ловит тихий реверт version-specific файла — компайл+javap+mixin-target-audit обязательны.**

Связь: [[reference-edit-routing-by-area]], [[reference-core-versioning]], [[tool-build-trilogy-script]], [[project-port-1201]], [[project-port-1214]], [[project-editor-free-camera]].
