---
name: plan-port-perlight-v2
description: "09-16: код порта v2 на BBS 2.6 и автоматические проверки DONE; игровой этап у пользователя, computer-use запрещён; сборка 1.1.7."
metadata:
  node_type: memory
  type: project
  originSessionId: f1f5062a-37d2-4cde-92e4-88bc7936f999
---

ЗАДАЧА. Юзер хочет: у источника света (Point/Spot) свои настройки VL и Outline (тумблеры) И мульти-выбор РЕПЛЕЕВ, которых свет/аутлайн задевает. Всё это уже реализовано на другой машине (ZoGa) в пакете `C:\Users\qualet\Desktop\IRLights-PerLight-VL-Outline-v2` (README-ru.md, checks/implementation.md — читать первыми). Решение 09-16: ВЗЯТЬ ЕГО ДИЗАЙН и портировать на текущий master. Моя альтернативная реализация (struct 160 Б) откачена, не воскрешать ([[project-per-light-profiles]]).

## Выпуск в Reff 2026-09-16 — актуальный статус

Пользователь разрешил «Коммитим», замену шейдеров Reff и сборку/установку 1.20.1. Уточнил: убрать код автоматических тестов Outline, не текст интерфейса. Удалены отдельные GPU-сценарии Outline из VlGpuTest; общие тесты сериализации/SSBO сохранены. Шаги порта закрыты; визуальный прогон именно Reff остаётся на пользователе, computer-use по-прежнему запрещён.

- Общий master поддерживает 1.20.1 и 1.20.4 через -Pmc; отдельной 1.20.1-ветки в addon/core нет, merge-коммит не создавался. Core main зафиксирован 40a68ae; аддон фиксируется текущим чекпоинтом.
- build -Pmc=1.20.1 PASS; profiles-bbs -MinecraftVersion 1.20.1: 1228 PASS; GPU VL/surface: 330 сравнений + profiles/linking PASS; round-trip 434 shader-файла PASS; nested-core 1.1.7/Java17 PASS.
- В Reff установлен irlite-1.1.7+mc1.20.1.jar (SHA256 21A15B020B250470C0CFC4FA13C416E9E7401DE5DCB460982D33134049A32BE5), standalone irl-core-1.1.6 заменён на 1.1.7. Сейчас BBS 2.6.1, Iris 1.7.6. Проверенные API пикера/треков/mixin-целей сохранены в BBS 2.6.1. Старый консервативный silhouette audit-гейт разрешает только 2.6: на 2.6.1 ожидается fallback UNKNOWN, это не отключение NPC-батчинга; гейт в этом выпуске не расширялся без полного аудита силуэтов.
- Шейдеры C:/PrismLauncher/instances/Reff/minecraft/shaderpacks: 6 ZIP + Reff_Shaders заменены с сохранением имён, 7 настроек .txt сохранены побайтно. Старый пак точно совпадал с патчем 8f2a1f9, неперенесённых кастомизаций нет. Новый архив SHA256 A8B122FBDFFD278AF305300533D5C254B9E8C0A0058A4AAC72FAFA2096FC6192.
- NPC находится в ReffMediaUtils-1.3.1-1.20.1.jar (не в bbs.jar). Его реальный CrowdInstancingInjector проверен внешним Java-скриптом на всех стадиях старого/нового паков: 15 vertex-инъекций и 207 неизменённых стадий в обоих, bbsi lightmap/head/leg helpers сохранены. Не запекать bbsi на диск: они добавляются при загрузке Iris. Мод NPC не изменён; FPS/визуальная проверка Reff не выполнялась.
- Desktop/shadres содержит папку пака, 6 ZIP и настройки; Desktop/mods — аддон и core. Все копии проверены по SHA256.
- Постоянный бэкап: C:/PrismLauncher/instances/Reff/irlights-backup-20260916-033703; отчёт deployment.json там же. Проверки и одноразовые скрипты: build/reff-deploy-20260916-033703. Push не делался.

Исторические статусы ниже описывают предыдущие стадии и не отменяют этот выпуск.
## Состояние после восстановления 2026-09-16 (актуально)

Код порта в рабочем дереве сохранён предыдущей сессией cb128cc2; восстановительная сессия проверила его, добавила BBS 2.6 regression-harness и подготовила сборку. Шаги 1–5 DONE; шаг 6 автоматическая часть DONE, игровой тест OPEN; шаг 7 коммиты не делались. Прежние формулировки «на старте» ниже — история, не повод повторять перенос.

- ПОСЛЕДНЕЕ УТОЧНЕНИЕ 09-16: после вывода переключателей возле списков пользователь подтвердил «Работает», затем попросил Outline на реплее, которого нет в Lit Replays. Списки теперь ПОЛНОСТЬЮ НЕЗАВИСИМЫ: Lit Replays ограничивает diffuse/GGX; Outline Replays ограничивает только обводку. Пустой включённый Lit Replays больше не гасит Outline; выключенный Outline-фильтр игнорирует оба списка. Прежняя семантика пересечения ниже — история пакета, не актуальное требование. GLSL irlite_matchesOutlineReplay больше не вызывает irlite_matchesLightReplay, UI-подпись про ограничение Light-списком удалена. Обновлены каноническая либа, генерируемый CR-патч и regression GPU-тесты. Новый тест сначала упал на старом шейдере (disjoint lists: vacuous image), после изменения PASS; build/vl-gpu-independent-replays/{report,profiles}.json: RTX3060, 64×32, 330 VL сравнений + независимость diffuse/GGX/Outline, пустые/непересекающиеся списки и отключение фильтров. Patch round-trip 434 PASS, build PASS. Пользовательские фильмы/глобалы не менялись, core прежний. Новое разделение списков ещё требует пользовательского визуального подтверждения.
- core main: 1.1.7, 96-байтные источники + 112-байтный хвост профилей; publishToMavenLocal PASS. Старый эксперимент со stride 160 не возвращался. VlGlobalsBuffer с surface-wave 2 сохранён.
- addon master: LightForm/LightEffects, LightEffectsRegistration, ReplayOutlineContext, LightReplayWidgets/UILightEffectsPanel, отдельная категория Light и группы TrackCatalog. В BBS 2.6 используется штатная STRING-фабрика ключей; свои редакторы зарегистрированы через bbs-client-addon/RegisterKeyframeEditorsEvent. Не добавлять ненужную common keyframe-фабрику.
- По запросу пользователя 09-16 собственный ReplayMultiPicker УДАЛЁН. Оба списка (Light/Outline) в форме и ключах используют UIAnchorKeyframeFactory.displayActors — тот же метод BBS 2.6, что UIOrbitClip/UITrackerClip. LightReplayPicker — только адаптер данных и callback штатного UISimpleContextMenu, без своего UI: клик переключает stable ID сразу, None очищает, меню остаётся открытым для нескольких выборов, выбранные строки имеют CHECKMARK. Корневые LightForm исключаются. Поиск штатный (>12 строк), Apply больше нет. Build PASS, profiles-bbs 1228 PASS, nested-core PASS; старого класса в JAR нет. Визуальный тест этого изменения OPEN, client после изменения не запускался.
- Перед заменой пикера пользователь сообщил, что списки не ограничивают свет/Outline. В сохранённом run/saves/Flat/bbs/films/3.dat списки содержали правильный stable ID, но selected_light_replays и selected_replays были false. Пользователю объяснено, что фильтры включаются отдельно. Исправление рендера не делалось и проблема не объявлена решённой. Оба shaderpack (новый и старый с суффиксом 1) совпали с эталонной либой; пользовательские фильмы/настройки не менялись.
- Следующий тест пользователя (03:12): свет на списки реагирует, Outline нет, локально выключить обводку не выходит. Текущий фильм уже 1.dat (3.dat удалён пользователем, не восстанавливать): selected_light_replays=true, selected_replays=false, custom_outline=false, глобальный outline=true; outline_replays в ключе tick0 пустой. Весь активный shaderpack совпал с Modification, не только либа. Исправлена доступность управления: UIReplaySelectionKeyframeFactory показывает тумблер фильтра всей формы; для Outline также Own outline settings/Outline и статус наследования/пересечения Light-списка. Формы/ключи показывают Filter OFF: list ignored. Неактивные персональные Outline-параметры заблокированы. Build и BBS 1228 PASS. GLSL/core/сцену не меняли. Вопрос пользователю «проверял ли с selected replays only ON» пока без ответа; не считать рендер починенным без такого теста. Старый GPU-harness тестировал сам outlineInk с синтетическими токенами, не запись/чтение colortex9 в Iris.
- Уточнение к исходному плану: списки названы outline_replays/light_replays; формат {film,replays:[stableId,...]}. BBS 2.6 хранит сущности по строковым replay.getId(), не по индексам. Формат v2-пакета для BBS 2.3.1 по индексам не мигрирует автоматически; старые фильмы без профилей читаются. Quality-поля отсутствуют на форме и всегда заполняются глобалами.
- Только Complementary. Генератор и патч совпадают с Modification; surface-wave 2/vlG/IRLITE_SURFACE_OK сохранены. 434 shader-файла после round-trip совпали. run/shaderpacks/ComplementaryReimagined_IRLights и run/irlite/patches уже совпадают с эталоном; менять их повторно не нужно.
- Новые tools/verify-profiles-bbs.ps1 + tools/profiles/{ProfilesBbsTest.java,classpath.init.gradle}: 1228 проверок PASS, реальные классы BBS 2.6 (копия/старые формы/save-load каналов/numeric midpoint/ступенчатые списки/reset/группы/глобальное качество). Подменён только singleton BBSMod; setup фабрик и TrackStyle обязателен при запуске без игры. Classpath экспортируется из Gradle, старые BBS из кэша не подмешиваются.
- tools/verify-vl-patch.ps1 получил -MainOnly для аддонного скоупа. Полный режим editor+DOF сохранён, эти проекты не менялись.
- Отчёты предыдущей сессии проверены на соответствие текущим исходникам: build/vl-gpu-profiles/{report,profiles}.json PASS (330 сравнений, смешанные профили, replay surface/GGX, max error 2.98e-8); core build/upload-gl-profiles/{report,profiles-registry}.json PASS (2305073 + 18073 проверок). Без изменения GPU-кода повторно не гонялись.
- Собран build/libs/irlite-1.1.7+mc1.20.4.jar; nested core ровно один и совпадает с core/build/libs, Java major 61; встроенный CR-патч совпадает с patches. Отчёт build/port-perlight-v2/bundle-verification.json. Готовый shader zip: build/distributions/ComplementaryReimagined-PerLight-v2-BBS2.6.zip. Сводка и ручной чеклист: docs/per-light-v2-port.md.
- Явное указание пользователя в этой сессии: computer-use ЗАПРЕЩЁН. Игровые/визуальные проверки пользователь делает сам; допустимы внешние скрипты. Восстановительная сессия клиент не запускала, UI не управляла. Осталось проверить реальные mixin/entrypoint в игре, обе панели и категорию Light, списки, прозрачные формы, визуал и FPS. Не объявлять это проверенным по headless-тестам.
- Память Claude восстановлена: общий MEMORY.md был усечён до 144 байт, план и project-per-light-profiles отсутствовали. Восстановлены из repo/memory; старый полный индекс сохранён в _index-recovery-2026-09-16.md, текущий сокращён до <15 КБ без удаления тематических файлов. Бэкап обоих индексов: build/port-perlight-v2/memory-backup. Junctions не менялись.

Следующий шаг: пользовательский runClient на дискретной GPU (команда в docs), затем исправить найденное и предложить коммит-чекпоинт. Без подтверждения не коммитить/пушить.

## Что в пакете (проверено 09-16)
- `irlightsbbs-addon/irlightsbbs-addon-sources.zip` (326 файлов), `irl-core/irl-core-sources.zip` (98), `Complementary/complementaryreimagined.irlights` + готовый zip пака, jar'ы 1.2.0-profiles.2 (MC 1.20.4, BBS 2.3.1), checks/*.json (все passed), logs. Распакованные копии лежали в scratchpad прошлой сессии — распаковать заново.
- БАЗА пакета = наш коммит 3dd4e33/8f2a1f9 (15.09 утро): ЕСТЬ силуэт-мост BbsSilhouetteBridge и stage-1a ядра (CasterRevision, ShadowOverlayCache); НЕТ порта на BBS 2.6 (4ccb9f5), НЕТ surface-wave 2 (d0ae0c0, 467ece3: setSurface/vlG/категория lighting). Их дельта на общих файлах мала: UISpotlightFormPanel 154 строки, UIPointLightFormPanel 104, SpotlightForm 36, PointLightForm 22, SpotGuideDrag 17, FormUtilsClientMixin 8, LightCollector 4 (+1-2 в UI*Form, SpotlightFormRenderer, GameRendererLightMixin, BbsSilhouetteBridge).
- НОВЫЕ файлы аддона: main/forms: LightForm (общий базовый класс Point/Spot: color/intensity/beam/aniso/density/bulb/entities_only/blocks_only/shadows), LightEffects (ValueGroup, листья регистрируются плоско на форме: custom_vl, vl_enabled, vl_intensity, vl_steps, vl_max_dist, vl_shadows, vl_shadow_stride, vl_tip_boost, vl_tip_radius, vl_noise, vl_noise_amount, vl_noise_scale, vl_noise_speed, vl_noise_morph, vl_noise_stride, custom_outline, outline, outline_target, outline_strength, outline_pixel_size, outline_fresnel, outline_back, outline_front(+_strength), outline_glow(+_strength), selected_replays, replay_film, replay_indices, selected_light_replays, light_replay_film, light_replay_indices), ValueReplaySelection (ValueString: JSON «фильм+индексы», ступенчатый). client: light/LightEffectsRegistration (форма -> LightProfile), light/ReplayOutlineContext (тег реплея на отрисовку: IdentityHashMap IEntity->token, beginFrame собирает контроллеры фильмов, render() флашит entity-батчи на границе тега, исключает ui/picking/iris shadow/bake), light/ReplaySelection, ui/ReplayMultiPicker (контекст-чеклист: поиск/Все/Сбросить/Применить), ui/UIReplaySelectionKeyframeFactory, ui/LightReplayTracks, panels/UILightFormPanel + UILightEffectsPanel + LightUiText (EN/RU), mixin: UIReplaySelectionFactoryMixin (createPanel HEAD), UIReplaysLightCategoryMixin, bbs/ReplayCategoryMixin+Invoker (расширение enum IRLITE_LIGHTS), iris/ReplayUniformMixin (CommonUniforms.addDynamicUniforms TAIL: uniform1i irlite_replayId).
- НОВОЕ в ядре: LightProfilesBuffer (61 строка): профили дописаны в ТОТ ЖЕ SSBO 7 хвостом — записи света 96 Б и header не тронуты; маркер header byte 12 = 0x49524C50 (IRLITE_PROFILES_OK); профили 112 Б × 2048 с OFFSET = 16+2048*96 = 196624; переменный массив target'ов (списки реплеев) с 426000; LightProfile (112 Б: 6 vec4 зеркал VL/Outline глобалов + meta: flags bit0 VL-override, bit1 Outline-override, bit2 outline-linking, bit3 light-linking; target offset; outline count; light count). Другие 6 паков читают старый префикс и работают без изменений. LightRegistry/LightBuffer/VlGlobalsBuffer — правки 9-43 строк.
- ШЕЙДЕР (только Complementary): дельта 292 строки vs наша либа до wave 2: `IrliteProfile irlite_profiles[2048]; uint irlite_replayTargets[];` в блоке SSBO, `irlite_profileMeta(i)`, `irlite_matchesLightReplay(meta, replay)`, own ? irlite_profiles[i].c/e/f/a : irlite_vlC/E/F/A. Тег реплея: gbuffers_* пишут `gl_FragData[N] = vec4(float(irlite_replayId), gl_FragCoord.z, 0, 1)` в colortex9 (RG32F, clear, blend off — ~25 операций патча по gbuffers_entities/hand/block/terrain/water/...), composite читает `texelFetch(colortex9,...).rg` для outline, surface берёт uniform напрямую. Iris 1.7.2 требует 4-компонентный colortex9ClearColor.
- СЕМАНТИКА: linking ограничивает diffuse+GGX+outline; VL-луч в воздухе не фильтруется; оба фильтра -> пересечение для outline; включён+пустой список = никого; выключен = список хранится, не ограничивает; кастеры теней не зависят. Списки = film id + zero-based индексы реплеев (семантика Orbit), едут при перестановке реплеев. Реплеи с корневой формой-светом исключены из выбора.

## Что портировать иначе (не копировать слепо)
1. BBS 2.6 вместо 2.3.1. Их UI-миксины бьют в внутренности 2.3.1: `collectFormPropertySheets` (в 2.6 НЕТ — треки строит TrackCatalog.of/ordered -> UIReplaysEditorUtils.buildSheets), `categoryOf(UIKeyframeSheet)` (в 2.6 есть + статический categoryOf(TrackId, owned)), `updateChannelsList`, `tabButtons`, `iconBar` (есть). Расширение enum ReplayCategory: ctor в 2.6 (Icon, IKey, IKey) — invoker вероятно совместим, проверить. АЛЬТЕРНАТИВА, уже сделанная в дереве: TrackCatalogMixin (RETURN TrackCatalog.of) + LightTrackLayout — имена/цвета/иконки/группы-заголовки (kind=BODY_PART) для треков света. Решить: своя категория «Свет» (их путь) или группы внутри FORM (мой путь) — можно оба.
2. Редактор ключа списка реплеев: в 2.6 есть чистое API — `RegisterKeyframeEditorsEvent.registerProperty("replay_indices", factory)` из entrypoint `bbs-client-addon` (класс implements BBSAddonMod, @Subscribe public void m(RegisterKeyframeEditorsEvent)); вместо их UIReplaySelectionFactoryMixin на createPanel. Свой keyframe-тип регистрируется через RegisterKeyframeFactoriesEvent (common, `bbs-addon`) — ключ с namespace; KeyframeChannel пишет ключ фактори через CollectionUtils.getKey(KeyframeFactories.FACTORIES).
3. НЕ переносить в персональные «графические» ручки (юзер 09-15): vl_steps, vl_shadow_stride, vl_noise_stride, outline_pixel_size, (vl_intensity спорно — оставить). Скрыть из UI/треков (BaseValue.invisible() или override isVisible()) — TrackCatalog и FormProperties.create показывают/создают треки только для isVisible(); загрузка/apply видимость не проверяют.
4. Surface-wave 2 уже в master: их либа/патч CR без vlG/IRLITE_SURFACE_OK — накладывать их 292-строчную дельту на ТЕКУЩУЮ Shadres/Modification/ComplementaryReimagined либу (не наоборот). Родные строки для якорей: SSBO-блок (irlite_pad2 -> маркер), irlite_outlineInk, irlite_lightSurface (irlMask), irlite_volumetric. Генератор tools/gen-complementary-patch.ps1 у них расширен «replay attachment helper» для gbuffers-опов — перенести.
5. Версии: их core 1.2.0-profiles.2 — параллельная ось; у нас ось 1.1.x (сейчас 1.1.6 в master/mavenLocal; 1.1.7 в mavenLocal — мусор от откаченной попытки, можно удалить). Бамп = 5 мест ([[reference-core-versioning]]).
6. LightForm как общий базовый класс Point/Spot — принять (у них FORM_ID/ключи свойств прежние, старые фильмы читаются; миграция profiles.1 нам не нужна).

## Порядок шагов
1. Распаковать оба sources.zip в scratchpad; `diff -r --strip-trailing-cr` их src против `git show 3dd4e33:` — получить точную дельту (список выше).
2. Ядро: добавить LightProfile + LightProfilesBuffer, правки LightBuffer(upload: prepare до glBufferSubData, маркер в header byte 12)/LightRegistry(копия профиля на регистрацию, компакция по cap/pending)/VlGlobalsBuffer; publishToMavenLocal (JDK17, ветка main); бамп координаты в addon build.gradle.
3. Аддон формы (main): LightForm/LightEffects/ValueReplaySelection; Point/Spot наследуют; регистрация keyframe-фактори.
4. Аддон client: LightEffectsRegistration + вызовы из LightCollector.emitPoint/emitSpot и обоих рендереров; ReplayOutlineContext + ReplayUniformMixin + хук render (у них через FormUtilsClientMixin/рендерер — смотреть их дельту AbstractLightFormRenderer? нет — смотреть FormUtilsClientMixin 8 строк и GameRendererLightMixin 1 строка = beginFrame); панели (их UILightFormPanel/UILightEffectsPanel + наш IrliteFormSections); ReplayMultiPicker + редактор ключа через registerProperty; треки: LightTrackLayout расширить их ключами (+ группа «Replays»).
5. Шейдер CR: наложить дельту на текущую либу + composite1 (colortex9 read) + pipelineSettings (colortex9Format RG32F, clear) + shaders.properties (blend.*.colortex9=off) + gbuffers-опы; gen-complementary-patch.ps1; round-trip PatchHarness (см. команды).
6. Проверки: compile (JDK21, -Pmc=1.20.4), GPU-харнесс tools/verify-vl-gpu.ps1 (их версия харнесса умеет профили/списки — взять из их tools), runClient CR на дискретке, глазами: тумблеры, списки реплеев, треки.
7. Затем предложить коммит-чекпоинты ([[commit-checkpoints]]): сначала оформление треков (уже в дереве), потом порт.

## Состояние дерева на старте (09-16, НЕ закоммичено)
- bbs-irlights-addon: НОВЫЕ src/client/.../ui/replays/LightTrackLayout.java, mixin/client/TrackCatalogMixin.java; УДАЛЁН mixin/client/UIReplaysEditorColorMixin.java (мёртвый: 2.6 берёт цвет из TrackStyle в каталоге); irlite.client.mixins.json (ColorMixin -> TrackCatalogMixin); memory/*. Компилируется против core 1.1.6. LightTrackLayout содержит стили и для будущих ключей (custom_outline, vl_*, outline_*) — ключи их LightEffects частично другие (outline_fresnel vs outline_fresnel_power) — подправить при порте.
- irl-core: чисто (HEAD). Shadres/Modification, patches, run/shaderpacks CR, run/irlite/patches = состояние master.

## Готчи и команды
- JDK: core `export JAVA_HOME="C:/Program Files/Eclipse Adoptium/jdk-17.0.20.8-hotspot"; ./gradlew publishToMavenLocal -Dorg.gradle.jvmargs=-Xmx3G`; аддон/клиент `export JAVA_HOME="C:/Program Files/Eclipse Adoptium/jdk-21.0.12.8-hotspot"; ./gradlew compileJava compileClientJava -Pmc=1.20.4` / `runClient -Pmc=1.20.4 -Pquickplay=Flat` (мир Flat; лог run/runclient-console.log). После смены координаты core чистить `.gradle/loom-cache/remapped_mods/remapped/org/qualet/irl-core-*/<ver>`.
- Патч round-trip: javac (JDK17, --release 17) 5 классов ../irl-core/src/main/java/org/qualet/irl/patcher/{IrlPatch,IrlPatchParser,IrlPatchApplier,PatchResult,PatchEngine}.java + tools/PatchHarness.java; `java PatchHarness patches/<p>.irlights Shadres/Original/<Pack> <out>`; удалить <out>/irlite_patched.txt; `git -c core.autocrlf=false diff --no-index --ignore-cr-at-eol <out>/shaders Shadres/Modification/<Pack>/shaders` = пусто. Генераторы: `powershell -NoProfile -ExecutionPolicy Bypass -File tools/gen-<p>-patch.ps1`.
- GPU-харнесс: `powershell -File tools/verify-vl-gpu.ps1 -JavaHome <JDK21> -GradleUserHome C:/Users/qualet/.gradle -OutputDirectory build/vl-gpu-x` (LWJGL 3.3.2 в ~/.gradle).
- Intel UHD (встройка) НЕ компилирует deferred2 с `readonly restrict buffer` при #version 130 (P5-эксперимент в либе) — на дискретке (юзер переключает) проходит; для теста на встройке убирать `readonly` в копии пака в run/shaderpacks.
- Правки 7 либ скриптом: построчно с сохранением EOL (Photon CRLF, Solas mixed) — образец scratchpad edit_glsl.py прошлой сессии (Doc класс: find/find_block/replace_block/insert_after по телу строки без EOL).
- BBS 2.6 API-факты: TrackCatalog.of(Form, FormProperties)/ordered, TrackDescriptor record (id, channel, owner, title, icon, color, property, seed, parent), TrackKind.BODY_PART = header-строка (не сохраняется, prune пустых), TrackStyle.register(property, icon, color) — глобально по имени ключа; UIKeyframeFactory.register/registerProperty; BaseValue.invisible(); UIElement.setVisible (резайзеры пропускают скрытых); UIIconToggles.add(icon, label, getter, setter).labelRow(IKey); UICirculate; IrlPatch.CONTRACT_VERSION=1 (их патч @irlite 1 — контракт не ломается, т.к. записи света прежние).
- Правило коммитов: только по подтверждению юзера, по чекпоинтам.

Связь: [[project-per-light-profiles]] (что было сделано и откачено, BBS 2.6 API), [[plan-per-light-profiles-and-colored-shadows]] (перф-прикидка), [[addon-light-buffer-ssbo]], [[shader-irlite-glsl]], [[patcher]], [[feedback-addon-runclient-command]], [[reference-core-versioning]].

Дополнение 2026-09-16 02:30: по команде пользователя запущен runClient -Pmc=1.20.4 -Pquickplay=Flat (JDK21), без computer-use. Мир загружен, окно отвечает, GPU RTX 3060; в логе BBS 2.6, core/addon 1.1.7, ComplementaryReimagined_IRLights, панели dashboard построены. Фатальных mixin/GLSL-ошибок запуска не обнаружено; есть предупреждения базового Complementary про endFlashIntensity и свойства блоков новых MC. Ручной визуальный тест фич всё ещё OPEN. Лог run/runclient-console.log.
