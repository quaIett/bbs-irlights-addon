---
name: project-editor-redesign
description: "АКТИВНАЯ фаза (дир. юзера 2026-07-22): редизайн UI редактора irl-editor на ветке main. Скрины юзера = ЦЕЛЕВОЙ дизайн, НЕ текущий код. Заход 1 (UI редактора: скин+шрифт+раскладка) -> Заход 2 (кросс-репо: Outline+UBO, группы источников, Presets). Плюс build-рецепт editor main (core-main republish, JDK17, клавиша J)."
metadata:
  node_type: memory
  type: project
  originSessionId: 71ce0040-4b91-4839-adbf-314751421f0a
  modified: 2026-07-22T17:29:57.774Z
---

Редизайн UI редактора (irl-editor, папка `BBS/irlights`, пакет `org.qualet.irlredactor`), ветка **main** (транк редактора = main, НЕ master). Директива юзера 2026-07-22: дизайн редактора чиним ТОЛЬКО на main; порт-ветки редактора (1.20.1/1.21.1/1.21.4/1.21.11) — в самом конце по команде. Ветку под сессию НЕ создавать ([[feedback-no-per-session-branch]]); коммит по чекпоинтам ([[commit-checkpoints]]).

**КЛЮЧЕВОЕ: скрины юзера = ЦЕЛЕВОЙ дизайн (референс), НЕ текущий код.** Текущий редактор (до сессии) = ОДНО окно `##irl_panel` (правый док 360px, akцент magenta #E62E8B, шрифт minecraft.ttf пиксель); всё линейно: sourceList -> инспектор-collapsing-группы -> engineGroup/vlGroup/perfGroup -> футер. НЕТ: окна Settings с навом, поиска, групп источников, вкладок инспектора, подсистем Outline/Presets. Почти весь UI рисуется вручную через ImDrawList в `editor/Widgets.java`; акцент захардкожен в EditorStyle+Widgets+PatcherPanel.

План:
- **Заход 1 — только репо редактора (UI, без шейдеров/core):**
  - шаг 1 СКИН+ШРИФТ **DONE + commit f326670 (main)**: magenta->red **#E42B25** (ховер #FF3A33, текст на красном белый) в EditorStyle/Widgets/PatcherPanel; шрифт minecraft.ttf->Segoe UI (интерим; путь `assets/irl-redactor/fonts/inter.ttf` = авто-оверрайд БЕЗ кода — позже юзер кладёт Inter TTF туда); убрана drop-shadow текста (shadowText флэт, SHADOW-константа удалена); shape-AA включён (EditorStyle); minecraft.ttf удалён. Рантайм PASS (собрался+запустился exit0), юзер визуально принял.
  - шаг 2 РАСКЛАДКА: **2a** двух-оконный сплит — `##irl_panel` к ЛЕВОМУ краю (источники+инспектор + внизу кнопка «Настройки» + футер «IRLights · qualet»); новое ПЛАВАЮЩЕЕ окно «Настройки» (обычное movable ImGui-окно, тайтл+крестик, флаг `settingsOpen` ImBoolean, FirstUseEver справа-сверху) с навом 6 категорий: Пресеты/Волюметрика/Тени/Обводка/Авто-свет/Патчер + поле поиска. engine/vl/perf разложены по категориям: VL intensity->Пресеты; авто-свет = СВОЯ категория (реш. юзера); Тени = quality/blocks/cache/radius; Пресеты = vlIntensity/guides/perf-профайлер/hold-bake; Обводка = заглушка outlineSoon; Патчер = кнопка -> модалка. Cookie/gobo остаётся во вкладке инспектора. compileClientJava PASS; рантайм-проверка в процессе. **2b** инспектор: collapsing-группы -> вкладки (Размещение/Основные/Объём/Тени). **2c** Волюметрика в 2 колонки + фильтр поиска + полировка (пин футера/пустот).
- **Заход 2 — кросс-репо, реально новое:** Обводка (Outline: draw-on/strength/thickness/fresnel/rim/catch-light/inner-glow — новые поля LightConfig + проводка в шейдер обводки, «outline-опции в UBO» -> [[plan-settings-ux-and-ubo-migration]]); группы источников («Пещера (4)» — новая модель группы на PlacedLight/LightScene + DnD); мастер-пресет Quality + Beam style «Smoky» + глобальный Softness (новые поля). Затем финальный порт на порт-ветки редактора.

**Build/run editor main (ГОЧА — записать):** editor main = MC 1.20.4, **Loom 1.9(-SNAPSHOT), Java 17**, Gradle wrapper 9.2.0. Требует irl-core:1.1.3 из ветки core **main** (Loom 1.9) в mavenLocal. Если core на ветке 1.21.1 (Loom 1.15.5, остаток от сессии тиража аддона-1.21.1) -> билд падает: `Mod was built with a newer version of Loom (1.15.5), you are using Loom (1.9.2)`. Фикс: `git -C irl-core checkout main && gradlew publishToMavenLocal` под JDK17 (меняет mavenLocal на 1.20.4-core; для аддона-1.21.1 потом переопубликовать core с 1.21.1). JDK: `C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot` (+jdk-21.0.11.10); системный JAVA_HOME = JDK8 нерабочий -> JAVA_HOME задавать явно. runClient: `JAVA_HOME=<jdk17> irlights/gradlew -p irlights runClient`; редактор в мире = клавиша **J**. См. [[reference-core-versioning]], [[reference-edit-routing-by-area]]. Шрифт-глиф-рейндж = Latin+кириллица, «…»/«—» = тофу, в новых строках избегать -> [[reference-imgui-font-glyph-range]].

## Прогресс + ADDON-PARITY (обновл. 2026-07-22)

Коммиты на main редактора: **f326670** скин(red #E42B25 + Segoe→Inter), **ab231f7** 2a(2-окна: левая панель + плавающее окно «Настройки» + bundle Inter 18pt), **0f45f7f** 2b(вкладки инспектора Placement/Basic/Volume/Shadows, `Widgets.tab`), **bb2c588** (левая панель авто-высота + список растёт с числом источников, `LIST_RESERVE`), **925e1e8** (Волюметрика 2 колонки), **0a8769f** (пресеты Quality/Beam style `SettingsPresets`+`presetRow`/`syncPresetMirrors` + Патчер стал кнопкой внизу лево-панели [Настройки][Патчер] + окно «Настройки» открывается справа-по-центру), **f44eef0** (живой акцент `imgui/EditorTheme` + категория «Интерфейс» с color-picker; `Widgets.refreshAccent()`+`EditorTheme.applyNative()` per-frame в draw), **3414baa** категория **Outline** (главный перенос настроек — ФИНИШ). Заход 1 (UI редактора + перенос настроек) по сути закрыт (8 коммитов на main).

**Директива юзера 2026-07-22: настройки редактора ~1:1 с BBS-аддоном** (аддон = источник правды). Карта аддона (`bbs-irlights-addon/src/main/java/qualet/irlite/{IrliteConfig,IrlightsAddon}.java` + `IrlitePresets.java`; модуль-настроек "irlights", 5 категорий):
- **Presets**: селектор **Quality**(Performance/Balanced/Quality/Ultra/Custom) + селектор **Beam style**(Clean/Dusty/Smoky/Custom); VL intensity(0-5 d1); **Max shader lights**(0-2048 d0 = LightRegistry.setUploadCap); Show guides; Debug: perf overlay. Индекс пресета НЕ хранится — выводится из значений (нет матча→Custom). Значения (в редакторе зеркалю 1:1): Quality(steps,maxDist,shadowStride,noiseStride,vlShadows,shadowQ,blocks): Perf=16/48/4/4/off/0/off · Bal=48/96/2/2/on/1/on · Qual=56/128/1/2/on/2/on · Ultra=64/192/1/1/on/2/on (Ultra НЕ ставит shadowQ=3 — 4.6GB VRAM). Beam(noise,amount,scale,speed,tipBoost,tipRadius): Clean=off/.6/2/.25/1/1.5 · Dusty=on/.6/2/.25/1.5/1.5 · Smoky=on/1/.5/3/2/2. `noise_morph` НЕ член пресетов.
- **Volumetric**: 13 опций — СОВПАДАЕТ с редактором (уже в 2 колонки).
- **Shadows**: shadow_quality(LOW/MED/HIGH/ULTRA d1) + block_shadows + **shadows_live**(Light shadows toggle) + **shadow_softness**(0-0.8 d.1). ⚠️ редактор сейчас = quality/blocks/**cache/radius** — НАБОР РАЗНЫЙ, нужна сверка (добавить shadows_live+softness; cache/radius в аддоне захардкожены, не UI).
- **Outline** (Wave1 миграции из Iris, 10 опций, live через UBO): outline, draw_on(ALL/ENTITIES/BLOCKS d1), strength(0-3 d.65), thickness/pixel_size(1-6 d6), fresnel_power(1-4 d2.2), back/rim(0-2 d1), front(toggle), front_strength(0-1.5 d.3), glow(toggle), glow_strength(0-.75 d.12). ❌ в редакторе НЕТ — Заход 2 (новые поля LightConfig + push через `VlGlobalsBuffer.setOutline(...)`, класс уже в irl-core).
- **Shader Patcher**. Auto-light — редактор-ЭКСКЛЮЗИВ (в аддоне нет).
- Проводка: `LightCollector`→`VlGlobalsBuffer` (binding7 UBO): set(...)/setOutline(...)/setShadow(shadowsLive,softness); флаги-битфилд bit0..6; скрытые всегда-on VL-флаги без UI: vlBlueNoise/vlClusterCull/vlShadowHiz. Лейблы аддона = code-registered `L10nMixin` (ключи `irlights.config.<cat>.<val>`); пресеты/debug/patcher = hardcoded `IKey.constant`.
- Стейл: `memory/addon-ui-config.md` описывает СТАРУЮ раскладку (irlite/irlite_patcher) — не верить, код перестроен в модуль "irlights".

**Сессия 2026-07-22 «настройки outline/VL/теней не реагируют» — ПОЧИНЕНО + КОММИТ 37d7166 (main, визуал PASS у юзера, runClient стартанул чисто):** ПРИЧИНА — боевой пак редактора `run/shaderpacks/ComplementaryReimagined_r5.8.1_IRLights` был устаревший (до-Wave1/Wave3: outline читался из compile-time `#define`, полей `irlite_vlE/vlF` нет, нет `IRLITE_SHADOW_SIZE_LIVE`), а bundled-патчи `src/client/resources/assets/irl-redactor/patches/` НЕ синканы с аддоном (`complementaryreimagined` 109870б/0 UBO-маркеров vs аддон 111775б/19). VL реагировал (vlA–vlD были в паке ещё до Wave1), outline/shadow — нет; Java-проводка (общий core, `LightDriver.collect` set+setOutline) была цела. ФИКС 3 узла: (1) `tools/copy-patches.ps1` — синк 7/7 патчей ← аддон (теперь MD5=аддон; патчи master==port/1.21.1, версия-агностичны); (2) standalone-перепатч боевого пака аддонским `tools/PatchHarness.java`: `javac -cp irl-core/build/classes/java/main -d <out> PatchHarness.java` → `java -cp "<out>;<core-classes>" PatchHarness <patch> <source.zip> <output_dir>` (validate 21 ops PASS; `IrlPatchApplier.apply` САМ сносит старый output→повторный прогон безопасен; `outputName`=strip.zip+`_IRLights`; движок патчера MC/Iris-free→standalone-safe); (3) проводка теней parity-1:1 с аддоном `LightCollector:175`: новые `LightConfig.shadowsLive`(bool d.true)/`shadowSoftness`(float 0..0.8 d.10) + `VlGlobalsBuffer.setShadow(...)` в `LightDriver.collect` сразу после setOutline + `shadowsCategory()` +2 контрола (`cfgShadowsLive`/`cfgShadowSoftness`) + lang EN/RU ключи `shadowsLive`/`shadowSoftnessDefault`. Гоча сборки ПОДТВЕРЖДЕНА: `republish core main` в mavenLocal ПЕРЕД `compileClientJava` (mavenLocal 1.1.3 был загрязнён 1.21.1/Loom 1.15.5 от прошлой сессии; после republish — Loom 1.9.2, BUILD SUCCESSFUL). ХВОСТ: (a) визуал-прогон юзера (клавиша J) — крутить outline/shadowsLive/softness в окне «Настройки»; (b) остальные 6 боевых паков редактора в `run/shaderpacks` тоже до-Wave1/Wave3 — перепатчить тем же harness при переключении активного пака (сейчас активен только Complementary по iris.properties).

**Остаток parity (докручено 2026-07-22, см. абзац выше):** пресеты DONE, **Outline DONE (3414baa)** — оказалось ТОЛЬКО редактор: `VlGlobalsBuffer.setOutline(...)` уже в core main (irl-core VlGlobalsBuffer.java:141), редактор уже пушил `VlGlobalsBuffer.set(...)` в `LightDriver.collect` (стр.51) — добавлен вызов setOutline рядом; 10 полей в `LightConfig` (outline/outlineTarget/Strength/PixelSize/FresnelPower/Back/Front/FrontStrength/Glow/GlowStrength, дефолты 1:1 аддон) + `outlineCategory()` UI. Сигнатура: `setOutline(enabled,target,strength,fresnelPower,back,front,frontStrength,glow,glowStrength,pixelSize)`; target 0 ALL/1 ENTITIES/2 BLOCKS. Осталось МЕЛКОЕ: Shadows-сверка (`shadows_live`+`softness` через `VlGlobalsBuffer.setShadow(enabled,size)` — тоже уже в core, стр.159) + Max shader lights (`LightRegistry.setUploadCap`). Акцент цвета пока НЕ персистится (сброс при рестарте). -> [[plan-settings-ux-and-ubo-migration]], [[addon-ui-config]] (стейл).
